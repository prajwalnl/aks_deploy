pipeline {
    agent any

    stages {
        stage('Build image') {
            steps {
                echo 'Build docker image'
            }
        }

        stage('Push image') {
            steps {
                echo 'Push docker image'
            }
        }
    }
}
